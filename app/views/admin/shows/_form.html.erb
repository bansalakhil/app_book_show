<%= form_for([:admin, @auditorium, @show]) do |f| %>
  <% if @show.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@show.errors.count, "error") %> prohibited this show from being saved:</h2>

      <ul>
      <% @show.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" >
    <%= collection_select(:show, :movie_id, Movie.order(:name), :id, :name) %>
  </div>

  <div class="field" >
    <%= f.label :on_date %>
    <%= f.text_field :on_date, :placeholder => 'eg: 20-12-2012' %>
  </div>


  <div class="field">
    <%= f.label :start_time %>
    <%= f.text_field :start_time, :placeholder => 'eg: 12:30:00' %>
  </div>

  <div class="field">
    <%= f.label :end_time %>
    <%= f.text_field :end_time, :placeholder => 'eg: 14:30:00' %>
  </div>

  <div class="actions">
    <%= f.submit :class => 'btn btn-primary' %>
  </div>
<% end %>
